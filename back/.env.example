# ============================================
# FleetCore - Variables de entorno (Ejemplo)
# Copiar este archivo a ".env" y reemplazar
# los valores con credenciales reales
# ============================================

# --- Configuración del servidor ---
MONGO_URI=mongodb://127.0.0.1:27017/fleetcore_dev
DB_NAME=fleetcore
PORT=5000
NODE_ENV=development

CLIENT_URL=http://localhost:5173

# API_URL=http://localhost:5000/api/v1

# --- Base de datos ---

# --- Autenticación Microsoft / Azure AD ---
AZURE_TENANT_ID=tu_tenant_id
AZURE_CLIENT_ID=tu_client_id
AZURE_CLIENT_SECRET=coloca_aqui_tu_secret
AZURE_REDIRECT_URI=http://localhost:5000/api/v1/auth/microsoft/callback

# --- JWT / Tokens ---
JWT_SECRET=coloca_un_secret_seguro
JWT_EXPIRES_IN=1d

# --- Email / Notificaciones (opcional) ---
SMTP_HOST=smtp.ejemplo.com
SMTP_PORT=587
SMTP_USER=tu_usuario
SMTP_PASS=tu_password

# --- Cloud storage (si usas Cloudinary o AWS) ---
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name
AWS_ACCESS_KEY_ID=tu_access_key
AWS_SECRET_ACCESS_KEY=tu_secret_access_key
AWS_BUCKET_NAME=tu_bucket

# --- Otras configuraciones ---
LOG_LEVEL=debug
